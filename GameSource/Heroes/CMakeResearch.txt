cmake_minimum_required (VERSION 2.6)
project{ ActorContainerAll }

get_filename_component { ACTOR_CONTAINER_ROOT "${CMAKE_CURRENT_SOURCE_DIR}" ABSOLUTE)

set(BUILD_SUPPORT_DIR "${ACTOR_CONTAINER_ROOT}/build-support")
set(PROJECT_CMAKE_DIR "${BUILD_SUPPORT_DIR}/opt/cmake")
set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${PROJECT_CMAKE_DIR}/modules/")

INCLUDE("${PROJECT_CMAKE_DIR}/macros/headless_setup.cmake")

set(CMAKE_INCLUDE_PATH "${BUILDY_BITS_ROOT}/LibUV/include/" )

include_directories(SYSTEM "${ACTOR_CONTAINER_ROOT}/3rdParty/DHCore/" )
include_directories(SYSTEM "/usr/include" "${BUILDY_BITS_ROOT}/LibUV/include")

add_subdirectory( ForestFireLocal )

cmake_minimum_required (VERSION 2.6)
project{ ForestFireLocal }

file ( GLOB_RECURSE
    ForestFireLocal_SRC
    "${CMAKE_CURRENT_SOURCE_DIR}/include/*.h"
    "${CMAKE_CURRENT_SOURCE_DIR}/source/*.cpp"
    )
    
set (
    ForestFireLocal_INCLUDES
    "${SDL2_ROOT}/SDL-2.0.3/include/"
    "${SDL2_ROOT}/SDL_ttf-2.0.12/include/"
    "${CMAKE_CURRENT_SOURCE_DIR}/include/"
    )
    
include_directories ( ${ForestFireLocal_INCLUDES} )

set (LIBS ${LIBS} ForestFire ForestFireClientInterfaces)

add_executable (ForestFireLocal ${ForestFireLocal_SRC} )

target_link_libraries ( ForestFireLocal ${LIBS} )